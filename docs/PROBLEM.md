# The problem statement

> Fill this in on the day, before writing any code. Everyone works from this
> file, so keep it current — it's cheaper than asking across a noisy room.

## Connection details

| | |
|---|---|
| **COT_URL** | `udp://________:______` |
| Transport | UDP / TCP / TLS |
| Confirmed working? | ☐ someone with ATAK has seen our marker |
| Confirmed by | (name, time) |
| Data package / certs | not needed for UDP |

```bash
export COT_URL=udp://________:______
```

## The problem, in one sentence

> _(Not a paraphrase of the brief — what are we actually making better, for
> whom? If you can't write it in one sentence you don't understand it yet.)_

## What we're building

- **Shape:** data-onto-the-map / react-to-the-map / both
- **Starting from:** `examples/______.py`
- **Data source:**
- **What appears on the map:**

## Success looks like

- [ ] Minimum: something real on the map, end to end
- [ ] Target:
- [ ] Stretch:

## Who's doing what

| Person | Machine | Branch | Callsign | UID prefix |
|---|---|---|---|---|
| | | | | |

## Demo plan

1. One sentence on the problem
2. Show the map (`python tak.py dashboard` on the big screen)
3. The moment that lands:
4. Honest limitation we name ourselves:

**Fallback:** record a good run with
`python -m taklib.server --record demo.cot`, keep
`python -m taklib.server --replay demo.cot --loop` ready in a spare terminal.

## Notes and decisions

_(Anything learned the hard way — put it here so nobody learns it twice.)_
